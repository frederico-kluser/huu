# Learnings — working-on-orchestrator

Append-only log consumed by meta-skill-evolution and meta-skill-consolidate.
Entry format: `- [YYYY-MM-DD][source:user|inference][task:<slug>][probation] <fact>`
States: probation (default) -> promoted (distilled into SKILL.md by meta-skill-consolidate after dual-buffer check) | superseded (kept for history, never deleted).
Learnings are routed here when THIS skill owns the domain of the fact — regardless of which skill ran the task.

<!-- entries below this line -->
- [2026-06-12][source:inference][task:memory-scope][probation] maxNodeExecutions caps cursor VISITS to steps, not per-file tasks (index.ts visitIndex increments once per cursor move) — a fan-out of N files is ONE node execution; width is bounded by file selection/maxFiles and the worker pool, not the node cap.
- [2026-06-12][source:inference][task:memory-scope][probation] On macOS, os.freemem() counts only truly-free pages, so ramPercent saturated ≥95% on any warmed-up Mac and AutoScaler.shouldSpawn() gated the pool FOREVER (agents pending, $0 cost) — root cause of 16 chronic orchestrator-test timeouts and stalled live runs. Fixed in resource-monitor.ts via vm_stat reclaimable pages (free+inactive+purgeable+speculative). Symptom signature to recognize: status running, activeAgents 0, cost 0, no spawn logs.
- [2026-06-12][source:inference][task:memory-scope][probation] New scope 'memory': pre-decompose at index.ts skips it; the lazy branch (revisit path) resolves filesFrom from manifest.integrationWorktreePath via orchestrator/memory-files.ts (missing→0 tasks stage-completes-empty; corrupt→run fails); task.hint feeds the $hint token in renderPrompt (replaced BEFORE $file).
- [2026-06-12][source:inference][task:dag-waves][probation] DAG mode (any dependsOn) runs via runDagWaves: BSP waves — computeWave (wave-scheduler.ts, pure) picks ready steps; their tasks share ONE executeTaskPool; merges are sequential in ARRAY order (deterministic). Ready checks are singleton waves. Outcomes/`next` are ACTIVATION edges: re-pend target + descendantsOf cone. Legacy cursor untouched when no dependsOn (next-as-skip lives only there).
- [2026-06-12][source:inference][task:dag-waves][probation] spawnAndRun now resolves its step from task.stageName (steps unique by topology) — the pool is step-agnostic, which is what makes multi-step waves share it. The visit bodies were extracted (prepareStageTasks / runCheckVisit / mergeStepVisit) and are shared by both modes; class-field narrowing (this.preflight etc.) needs `!` inside them.
- [2026-06-12][source:inference][task:error-ux][probation] Fatal-error policy: always go through recordRunError() (status+manifest.errorReason first-wins+log+emit) so the summary/headless surface the ROOT cause with an actionable hint. Side effect: error assignments now live outside start(), so TS flow-narrows this.status to 'running' at the epilogue — the `as OrchestratorState['status']` cast on statusAfterRun is deliberate (an annotated const does NOT escape CFA narrowing).
- [2026-06-24][source:inference][task:greedy-max-mode][probation] Adding a scaler mode beyond auto/manual touches TWO non-obvious spots besides AutoScaler: (1) the per-tick target recompute in executeTaskPool is gated by `getMode() === 'auto'` — a new mode that needs a dynamic instanceCount must be added to that condition or it stays frozen at its seed; (2) the mode's enable method MUST call `portAllocator.setMaxAgents(AUTO_SCALE_MAX_INSTANCES)` like enableAutoScale(), else real concurrency is silently pinned at the manual port window (MAX_INSTANCES=20) however high targetConcurrency() returns. Added 'greedy' (UI label MAX) this way: targetConcurrency early-returns min(active+pending, maxAgents), shouldSpawn gates at the destroy threshold (both CPU and RAM, to match shouldDestroy's OR) and honors COOLDOWN for damping.
- [2026-06-24][source:inference][task:greedy-max-mode][probation] AutoScaleStatus.enabled is `this.enabled && mode === 'auto'` — it means "auto is DRIVING the target", NOT "scaler is on". The memory guard (shouldDestroy → destroyAgent: kill newest by startedAt, requeue to front of TODO with a requeues++ counter, killedAgentIds skips retry accounting) is mode-agnostic and runs in the poll loop for EVERY mode, so a new mode inherits kill-newest+requeue for free with zero guard changes. Consumers that read `enabled` as a proxy for "is auto" break once a third mode exists — branch on `mode` instead.
- [2026-06-25][source:inference][task:card-action-counters][probation] To derive a per-agent accumulator from the agent event stream (e.g. per-action counts + lastAction for the kanban label), `handleAgentEvent` is the single funnel — bump it at the TOP, BEFORE the switch, via a map-mutate-WITHOUT-emit helper (`bumpAction`, mirrors `appendAgentLog`): read `this.agents.get(id)`, spread a new object, `this.agents.set`, and rely on `handleAgentEvent`'s trailing `this.emit()`. Ordering is safe because the switch's `updateAgentStatus`/`appendAgentLog` re-`get` a FRESH snapshot and spread `{...cur}`, so they preserve the bump. `state_change` must split into stream/tool (it carries `state`); orchestrator-driven phase changes (committing/pushing) go through `updateAgentStatus` directly and are NOT events, so they don't count. Such telemetry accumulators are NOT reset by the guard-kill requeue in `destroyAgent` (it only resets work fields — branch/files/error/timestamps), so they persist across requeues like tokens/cost/logs by default — matching, not breaking, the existing reset semantics.
- [2026-06-25][source:inference][task:run-log-realtime-stream][probation] Real-time agent output now flows through a SECOND subscription channel distinct from `subscribe` (which pushes THROTTLED state snapshots): `subscribeAgentOutput(cb)` — an append-only firehose of coalesced output lines `{agentId,channel,text}`, NO history replay, used by the web server to mirror raw pi output to the browser console (server fans each chunk as an `agent-stream` SSE frame; throttled-snapshot `subscribe` is unchanged). New AgentEvent `stream` (channel assistant|thinking, delta) is handled in `handleAgentEvent` via per-agent/per-channel `StreamLineBuffer` coalescers (`orchestrator/stream-line-buffer.ts`, PURE: deltas→whole lines, trailing-CR stripped, force-flush past 4 KB so a newline-less blob can't stall). Non-obvious wiring: the `case 'stream'` RETURNS early (skips the trailing `emit()`) so per-token deltas don't each trigger a `getState()`; `emitStreamLine` runs once per COMPLETED line and fans the firehose for BOTH channels but only ASSISTANT lines also hit `this.log()`+`appendAgentLog()`+`emit()` (thinking is console-only — too verbose for the bounded 1000-line ring). Buffers flush on `done`/`error` (`flushStreamBuffers`) and are `.delete()`d right before each (re)spawn (retries/memory-requeues reuse the agentId → a stale partial line would otherwise prepend to the next attempt). The judge(9998)/integration(9999) `onEvent` handlers only act on log/error, so they silently ignore `stream` (their streaming isn't mirrored — possible follow-up). This also fixes the TUI/headless run log, not just the web, since both read the same `this.logs`.
- [2026-06-28][source:inference][task:test-suite-code-frozen][probation] By the time a CheckStep judge (or any post-stage step) runs, stage merges are already COMMITTED into the integration worktree, so `git status --porcelain` is CLEAN — a "no files changed" guard verifies almost nothing. To check what a RUN changed, diff against the run base. Plumbing added: `CheckEvaluationContext.baseCommit?` (optional — test fixtures omit it) surfaced from `this.preflight!.baseCommit`; `buildCheckSystemPrompt` prints it as a Git Context line AND `evaluateCheckStep` substitutes `$baseCommit` into the resolved condition (alongside `$runs`); `Orchestrator.renderPrompt` substitutes `$baseCommit` into work-step prompts. A step can then `git diff --name-only $baseCommit..HEAD` or `git checkout $baseCommit -- <path>`. Gotcha: a literal `$baseCommit` left UNSUBSTITUTED in a check condition expands to an empty shell var → `..HEAD` (empty diff, exit 0) → a freeze guard passes vacuously, so substitution + fail-closed wording are both required.
- [2026-06-26][source:user][task:realtime-cost-and-log-fixes][probation] SUPERSEDES the "thinking is console-only" half of [task:run-log-realtime-stream]: `emitStreamLine` now appends THINKING lines to the per-agent log too (tagged with `THINKING_LOG_PREFIX` = '🧠 ', a new value export in `orchestrator/types.ts`), on top of the firehose — so the web card drawer shows the same assistant+thinking stream the browser console mirrors. Thinking is STILL kept out of the global `this.log()` run log (too verbose there). `appendAgentLog`'s per-agent ring was raised 100→200 to match the web server's `MAX_AGENT_LOG_LINES`, so the drawer's live tail and `/api/agent-logs` agree now that reasoning shares the buffer. Also: `getState()` now returns `totalCost = +Σ agents.cost .toFixed(4)` instead of the hardcoded `0 // M5 will populate`; `SimulationEngine` mirrored (pushes a tagged 🧠 line into `card.status.logs`; its getState already summed card cost). The judge(9998)/integration(9999) onEvent handlers still ignore `stream`, so their reasoning isn't mirrored and their cost isn't summed — unchanged.
- [2026-06-30][source:user][task:memory-scope-resilient][probation] REFINES the 2026-06-12 [task:memory-scope] "corrupt→run fails" rule: the memory layer must NEVER abort a run for a SALVAGEABLE reason — only a file that is not a usable list AT ALL is fatal. So `memory-files.ts` no longer hard-validates per-entry shape: the schema is now `files: z.array(z.unknown())` (top-level still gates `_format` literal + `files`-is-array), and each entry is salvaged in a pure `normalizeEntry(entry, order, warnings)` helper — over-length hint TRUNCATED to `DEFAULT_MEMORY_HINT_MAX_CHARS` (new const in lib/types.ts, =600), non-string hint / non-numeric priority IGNORED, entry that isn't a non-empty string or `{ path:string }` DROPPED — every adjustment pushing a `warnings[]` line (surfaced as `warn` logs at index.ts ~2094, so salvage is visible). STILL fatal: invalid JSON, wrong/absent `_format`, `files` not an array, and the unchanged backstop `normalized.length>0 && usable.length===0` (named real-looking paths of which NONE survive). Trap: `normalized` now means "entries that yielded a usable path", so an all-garbage `files:[42,null]` → normalized empty → backstop does NOT fire → resolves to 0 tasks + warnings (stage completes empty), which is the desired "never block" behavior, NOT the throw the old strict schema gave. Root cause of the user's bug was doubly unfair: the MEMORY CONTRACT prompt (lib/memory-contract.ts) never told the producer about the 600-char cap — now it does ("truncated at run time, never rejected"). Pinned by new tests in memory-files.test.ts (truncate/ignore/drop) + the `oversized-hint` end-to-end case in memory-scope.test.ts (run status MUST be `done`).
- [2026-06-29][source:user][task:timeout-error-retry][promoted] USER-triggered retry of a failed task card. The load-bearing constraint: the integration worktree is torn down in `start()`'s `finally` (`removeIntegrationWorktree` near the end), so a retry that must MERGE its result has to happen while the run is still alive. Solution = a held-open state: new `OrchestratorState.status` value `awaiting_retry`, gated by `OrchestratorOptions.interactiveRetry` (default false → headless/run-many/sim/tests byte-identical, start() resolves immediately). After the step-walk `while` loop and BEFORE the terminal status logic, if `interactiveRetry && !aborted && status!=="error" && hasFailedCards()`, set `awaiting_retry`, emit, `await new Promise(r => this.finishResolve = r)` — INSIDE the try, so the finally (teardown) only runs after the gate resolves. `finish()` (idempotent; bails while `retryingIds.size>0` so teardown cannot race a merge) and `abort()` both resolve the gate. `retryTask(agentId,{timeoutMs?})` reuses ALL existing machinery: reconstruct the task from a new `tasksById` map (the CARD lacks `files[]`/`hint` — populate the map at the TOP of `spawnAndRun`, the single choke point), reset the card to pending (clear error/errorKind, bump new `AgentStatus.manualRetries`), `executeTaskPool([task])` (re-runs off `stageBaseRef` = current integration HEAD), then on `state==="done" && commitSha` push a synthetic StageIntegration card (visitIndex = RETRY_MERGE_VISIT_BASE 1e6 + seq, never collides with walk visitIndexes ≤ maxNodeExecutions) and `runStageIntegration([task],visitIndex)` to merge just that branch, advancing `stageBaseRef` so retries stack. Per-task timeout override = `Map<agentId,ms>` consumed in spawnAndRun (`retryTimeoutOverrides.get(id) ?? computeCardTimeoutMs(...)`). THREE traps: (1) flip status OUT of awaiting_retry (→running) BEFORE the reset-emit, else a subscriber keyed on awaiting_retry sees the card already cleared and prematurely finishes mid-retry; (2) DECREMENT completedTasks before re-running (the failure already counted it; the re-run re-counts once → net zero); (3) make appendManifestEntry REPLACE an existing same-agentId entry (a retry reuses the id → else a duplicate). errorKind ("timeout"|"failed") was ALREADY set on final-fail — only the UI/retry surface was missing. Pinned by `retry-task.test.ts`.
- [2026-06-30][source:user][task:fase1-ram-budget-psi-admission][promoted] Fase 1 of the "fill the machine, never crash" roadmap reworked the AutoScaler's three wrong axes after a 9-run web OOM (see memory huu-multirun-oom-incident). SIGNAL: `shouldSpawn()` now PSI-gates auto+greedy (NOT manual — user-pinned) — freezes admission when `SystemMetrics.memPressureSome10` (NEW field) ≥ `admitPsiThreshold` (default 0.5). PSI = `some avg10` from cgroup `memory.pressure` when containerAware else `/proc/pressure/memory`, via pure `parseMemoryPressureSome10` in resource-monitor.ts; `null` off-Linux/no-CONFIG_PSI → falls back to the RAM `stopThreshold` gate (degrade-never-block). ENVELOPE: `targetConcurrency`/`headroomCapacity` dropped the `available−margin` math for a RAM BUDGET via a private `budgetAdditional()` = `floor((ramBudgetBytes(ramTotalBytes, budgetPercent) − ramUsedBytes) / observedAgentBytes)`. `lib/budget.ts` (leaf): `ramBudgetBytes` = % of TOTAL, floored at `total − 512MiB`; dial is MACHINE-GLOBAL (`HUU_RAM_PERCENT`/`--ram-percent`/web Setting, default 85, clamp 10–95) → `OrchestratorOptions.budgetPercent` (single-run) or `GlobalScheduler.setBudgetPercent()` (multi-run; ONE shared budget AutoScaler, per-run ones dormant). SEED: `DEFAULT_AGENT_MEMORY_ESTIMATE_MB` 250→1536 (pessimistic; EMA corrects DOWN) — inverts the incident's admit-too-many: on a 31GiB/15.8GiB-used box, 85% headroom = ~7 agents now vs ~43 with the old seed (verified at runtime). RAMP: `executeTaskPool` caps NEW spawns/tick to `max(1, ceil(busy*0.5))` (geometric ~+50%/tick) unless `ramping = scheduler!=null || mode!=='manual'` — kills the single-tick burst while staying fast. `shouldDestroy` stays RAM/CPU 95% in Fase 1 (PSI-destroy is Fase 2). TEST GOTCHA: changing the default seed shifts every implicit-seed concurrency assertion — EMA tests must pin `agentMemoryEstimateMb:250`, and `global-scheduler.test.ts`'s "plentiful RAM" fixture was bumped 256→512 GiB so B still saturates maxAgents under the heavier seed. Pinned by auto-scaler.test.ts (budget% + PSI gate + 1536 seed) + budget.test.ts. Fases 2 (cgroup memory.high/max, senpai/AIMD controller, zram, pi session-persist) and 3 (agents as subprocesses, PSS, full degradation hierarchy) still pending.
- [2026-06-30][source:user][task:fase2.2-adaptive-psi-controller][probation] Fase 2.2 = closed-loop PSI controller in `auto-scaler.ts` (senpai/TMO + Netflix AIMD/Vegas). SUPERSEDES the `admitPsiThreshold` NAME and the freeze-at-setpoint half of the Fase 1 entry above. New private `controlledLimit` driven each poll by `updateController()`: PSI `some avg10` < setpoint (`targetPsi`, default 0.5, renamed from admitPsiThreshold) → **additive increase** `+max(3, ceil(0.1·limit))` (Vegas alpha); PSI ≥ cut band (`targetPsi × PSI_CUT_BAND_MULT=2` = 1.0) → **×0.5 cut** + `controllerHoldUntil = now+5s` (no re-ramp during the hold); setpoint ≤ PSI < cut band → **hold** (hysteresis). The RAM budget (`budgetCeiling()` = `active + budgetAdditional()`, capped maxAgents) is the HARD ceiling the controller clamps within. KEY CORRECTNESS SEAM — `effectiveLimit()`: returns `controlledLimit` ONLY when the controller is actively driving (`controllerEnabled && mode==='auto' && memPressureSome10 !== null`), ELSE the LIVE `budgetCeiling()` — because the GlobalScheduler calls `syncCounts()+targetConcurrency()` SYNCHRONOUSLY each tick and must see the new counts immediately (the poll-cached controlledLimit would lag → stale B). So open-loop (PSI null / disabled / scheduler-with-null-PSI) stays byte-identical to Fase 1; the closed loop only engages with real PSI. The shouldSpawn binary freeze MOVED from the setpoint to the cut band (`psiFreezeThreshold()`) so the controller can run the machine AT ~setpoint without the gate fighting it — this is what lets huu fill to higher utilization than Fase 1's conservative 0.5 freeze. `pollMetrics()` no longer early-returns in COOLDOWN (so `updateController()` always runs; it cuts even in cooldown but won't re-ramp while `state==='COOLDOWN'`). `AutoScaleStatus` gained OPTIONAL `controlledLimit?`/`targetPsi?` (optional so the simulation-engine literal doesn't break). TEST GOTCHAS: the Fase 1 PSI-freeze test moved 0.5→1.0 (freeze is now the cut band); controller tests use a `metricsRef` + `vi.advanceTimersByTime` on a BIG machine (256 GiB → ceiling=maxAgents 200) to isolate the controller from the budget ceiling, and assert RANGES for the additive re-ramp (poll-timing-dependent). Runtime-verified (PSI 0→3→0→0.7→5 sweep): 200→cut 100→cut 50→ramp 75→hold 75→cut 37. Nothing in `index.ts`/`GlobalScheduler` changed. Remaining Fase 2: 2.1 cgroup, 2.3 pi session-persist, 2.4 SQLite queue, 2.5 zram (ROADMAP.md).
- [2026-07-01][source:user][task:multi-run-list-order-priority][probation] Multi-run PRIORITY (which project's cards get pulled first — the user's ask: "start from the FIRST project in the list, backfill onward, to finish projects gradually") was assigned by the ORDER `GlobalScheduler.register()` is CALLED, which is a RACE: all three front-ends start their runs CONCURRENTLY and register() runs only AFTER each run's async preflight (index.ts ~1125), so preflight-completion order — NOT list order — set the seq/priority. The pure `distributeBudget` (top-down by priority) + `selectGlobalVictim` (reverse) were ALREADY correct; only the seq SOURCE was wrong. FIX = caller-authoritative priority: `RunSlot` gained a `priority` field (lower=higher, DEFAULTS to `seq` → single-run/legacy byte-identical), `register(driver, priority?)` stores `priority ?? seq`, and BOTH `recomputeGrants` (asc) and `selectGlobalVictim` (desc) now sort by `(priority, seq)` with seq the stable tie-break. Plumbed via new `OrchestratorOptions.priority` → `this.schedulerPriority` → the register() call's 2nd arg. Every multi-run construction site passes its LIST INDEX: `run-many` admitOne `priority:i`, TUI `MultiRunDashboard` `pipelines.map((p,i)=>…priority:i)`, web `WebRunManager` `params.priority ?? this.enqueueSeq++`. Single-run sites (`RunDashboard`, `headless-run`) never bind a scheduler → register() never fires → priority moot (grep `new Orchestrator(` + `scheduler` to confirm coverage). backfill-vs-strict-sequential was the user's EXPLICIT call = BACKFILL (keep filling idle slots; "em diante"), so `distributeBudget` is UNCHANGED — only the priority SOURCE was hardened. Pinned by 3 new `global-scheduler.test.ts` cases (highest-priority run registered LAST still served first; lowest-priority registered FIRST still the victim; omitted priority → registration-order fallback).
- [2026-06-30][source:user][task:fase2.3-pause-instead-of-kill][probation] Fase 2.3 = the memory guard PAUSES its victim (preserve worktree+branch+pi transcript, free RAM) and RESUMES it later, instead of killing+restarting-from-zero. Built as a strict OPTIMISATION over `destroyAgent` with a GUARANTEED fallback ⇒ zero regression by construction. ARCHITECTURE: new OPTIONAL `SpawnedAgent.checkpoint?(): Promise<string|null>` (pi: `session.sessionFile` if `existsSync && size>0` else null — does NOT abort/dispose; the caller disposes next) + `AgentRuntimeContext.restoreSessionPath?`. `pauseAgent(id)` mirrors `destroyAgent` BUT: `await checkpoint?.()` → **null/absent/throw ⇒ `await destroyAgent(id); return`** (kill fallback); else add to a consumable `pausedAgentIds` Set (sibling of `killedAgentIds`) BEFORE dispose, dispose, PRESERVE worktree+branch (do NOT remove), record `restoreSessionPaths.set(id,path)`, phase `'paused'` + `pauses++` (NOT `requeues++`), `pendingTasks.unshift(task)`. RESUME is FREE — the task sits in `pendingTasks` and the existing `shouldSpawn` gate admits it when headroom returns; spawnAndRun's FIRST attempt, if `restoreSessionPaths.has(id) && existsSync(task.worktreePath)`, REUSES the worktree (skip `createAgentWorktree` — it would fail, branch+worktree exist) and threads `restoreSessionPath` into the factory's runtimeContext (consume the record one-shot; a failed resume degrades to a fresh attempt). pi factory `SessionManager.inMemory()`→`SessionManager.create(cwd, sessionDir)` / `SessionManager.open(restorePath, undefined, cwd)` for resume. SIX traps, each load-bearing: (1) **RunDriver is an EXPLICIT WRAPPER** at index.ts ~1113 (`{runId,getDemand,activeAgentAges,destroyAgent,acceptMetrics}`), NOT `this` — a new public method (`pauseAgent`) is INVISIBLE to the GlobalScheduler until added to that literal; the omission silently falls back to kill and a multi-run pause test caught it (bPauses=0). (2) **sessionDir MUST be OUTSIDE the worktree** — `join(dirname(cwd),'.huu-sessions',basename(cwd))` (a sibling under the run base); inside `cwd` the finalize `git stageAll` COMMITS pi's transcript into the user's repo. (3) **dispose makes prompt() reject OR resolve** depending on backend, so spawnAndRun intercepts `pausedAgentIds` in BOTH the post-`prompt()` success path AND the catch (`killedAgentIds.delete(id) || pausedAgentIds.delete(id)` → spawningIds.delete + return) — only one fires. (4) The pool stays alive on `pendingTasks` (`executeTaskPool` while-cond), so a paused task in the queue keeps the stage from merging ⇒ `stageBaseRef` CAN'T advance under a paused agent ⇒ base-staleness is STRUCTURALLY impossible (no HEAD snapshot needed). (5) session files leak unless cleaned — `removeAgentWorktree` now also rm's the sibling session dir (co-located cleanup; pauseAgent deliberately skips it to preserve), + `cleanupAll`/`cleanupRunFromManifest` wholesale-rm `<base>/.huu-sessions`. (6) the `HUU_NO_PAUSE=1` escape hatch is read at construction into a `pauseInsteadOfKill` field on BOTH guards — the Orchestrator (single-run, index.ts guard) AND the GlobalScheduler (multi-run, `enforceMemoryGuard`: `pauseInsteadOfKill && driver.pauseAgent ? pauseAgent : destroyAgent`). FALLBACK COROLLARY: an agent/mock WITHOUT `checkpoint` (azure, stub-less-test mocks) auto-gets the kill path — which is why every pre-existing requeue/multi-run test stayed green untouched. UI: `AgentLifecyclePhase 'paused'` → `pickColumn` DONE area, `lifecycleStatus` amber `PAUSED` (NOT magenta — reserved for AI), `⏸N` title badge next to `↻N`; web `agentCard` paused lane + `.phase.paused` token; NOT added to the (dead) `ACTIVE_PHASES`. VALIDATION: P0 runtime spike vs the REAL pi SDK PROVED checkpoint→abort-mid-task→`open`→`prompt("continue")` doesn't throw, the JSONL stays parseable, and the agent does NOT redo completed tool calls (step1 mtime unchanged, step2 produced); a real Orchestrator+pi run confirmed no transcript leaks into the commit. Pinned by requeue.test.ts (pause→resume reuses worktree + restoreSessionPath threaded + pauses=1; checkpoint-null → kill fallback) and multi-run-priority.test.ts (lowest-priority run PAUSED not killed; HUU_NO_PAUSE=1 reverts to kill). SPIKE GOTCHA (any standalone script resolving the OpenRouter key): the SAVED `~/.config/huu/config.json` key OUTRANKS `$OPENROUTER_API_KEY` (api-key.ts precedence) — a stale env key gives `401 User not found`; resolve config-first. Remaining Fase 2: 2.1 cgroup, 2.4 SQLite queue, 2.5 zram (ROADMAP.md).
- [2026-07-01][source:inference][task:judge-verdict-stream-capture][probation] CheckStep judges ALWAYS fell back to their `default: true` outcome with the real pi backend — a load-bearing bug hidden for the whole life of the feature. `evaluateCheckStep` (`src/orchestrator/check-evaluator.ts`) builds the verdict text from an `onEvent` closure that pushed to `collectedText` ONLY on `event.type === 'log'`, then `extractVerdict` parses the last `{ "label", "reason" }` JSON block from it. But the pi `event-mapper` (`backends/pi/event-mapper.ts`) surfaces the judge's ANSWER as `type:'stream', channel:'assistant'` deltas — `message_end` emits only a token-count `log`, never the answer text — so the verdict block was never in `collectedText`. Result: `extractVerdict` returned null → the fallback (default outcome) fired every time, so every judge gate in every bundled audit pipeline was a silent no-op. INVISIBLE to tests because the stub factory (`check-runs.test.ts`, `check-evaluator.test.ts`) emits the verdict as a `log` — the exact backend-shape gap the stub can't cover. Fix: also `collectedText.push(event.delta)` for `stream`/assistant events (thinking channel EXCLUDED so its reasoning can't spoof a verdict). Diagnosis tell: the judge "approved via default" on BOTH gpt-4o-mini AND claude-sonnet-4 — identical behaviour across model tiers means the verdict is never being READ, not that the model is too weak to WRITE it. Lesson: when a real-backend behaviour is only ever exercised by the stub in tests, confirm the stub emits the SAME event TYPES the real backend does (here: log vs stream) before trusting green tests.
- [2026-07-01][source:user][task:crash-guard-resilience][probation] A SINGLE uncaught error killed the whole multi-run web server (5 concurrent runs + the HTTP server) — the app "froze" (was actually DEAD; `tsx --watch` from `npm run dev` doesn't relaunch on crash, only on file change, so it looked hung). Trigger: a detached `pi-animations` `setTimeout` touched a stale pi session ctx after session replacement ("This extension ctx is stale after session replacement or reload") → `uncaughtException`. The process-death surface had THREE registered uncaught/unhandled handlers: `debug-logger.ts` (logs `ev:uncaughtException`, no exit), `ui/safe-terminal.ts` (logs `safe_uncaught` + `process.exit(1)`), and `cli.tsx` (stderr + `process.exit(1)`) — on an uncaught, ALL fire and whichever `exit()` runs first wins, so the process always died. Per-run AWAITED errors were ALREADY isolated (`run-manager.ts beginRun().catch()` marks only that run errored); only ASYNC/uncaught errors (detached timers, unhandled rejections) escaped to the top. FIX: new `src/lib/crash-guard.ts` is the SINGLE authoritative handler with a mode switch — FATAL by default (one-shot `huu auto` + TUI exit loud) and RESILIENT once `web/serve.ts` calls `setResilient(true)` after the server is LISTENING (startup errors stay fatal). In resilient mode an uncaught is logged (deduped by signature) + CONTAINED; a wedged run self-recovers via its per-card timeout. A real meltdown still bails via a pure `isErrorStorm(entries, now, {windowMs, distinctMax, totalMax})` — exits on many DISTINCT sigs in the window (broad cascade ⇒ corruption) OR an extreme total rate (CPU-pin loop), but a SINGLE benign error repeating forever is survived. Had to REMOVE the two exiting handlers (cli.tsx + safe-terminal) — a sibling `process.exit` pre-empts the guard's survive decision. Lesson: in a multi-run SINGLE-PROCESS host, `process.exit`-on-uncaught is a single point of failure for the entire fleet; centralize the exit-vs-survive policy in ONE mode-aware guard and forbid sibling exit handlers. Env knobs: `HUU_CRASH_STORM_WINDOW_MS`/`_DISTINCT`/`_TOTAL`. Proven end-to-end (tsx script: detached-timer throw + unhandled rejection contained, process survives; fatal mode exits 1) + `crash-guard.test.ts` unit-tests the storm logic.
- [2026-07-01][source:user][task:ram-utilization-investigation][probation] "O dial de 85% de RAM nunca é atingido" — investigação completa da cadeia dial→concorrência provou que o gargalo dominante NÃO é o teto: é DEMANDA. Ordem de dominância: (1) fan-out dos pipelines — huu Test Suite paraleliza em SÓ 1 de 6 passos (`TARGETS_MAX_FILES`, era 12 → agora 24) e `B = min(effectiveLimit, demandaTotal)` é demand-capped por construção (auto-scaler targetConcurrency; distributeBudget), então na maior parte do wall-clock não há agentes pedindo slots; (2) merges/judges seriais derrubam `getDemand()` (= active+spawning+pending; integração/judge reservados NÃO contam) a ~0 entre estágios; (3) o seed pessimista 1536MiB (anti-OOM deliberado) limita a máquina a ~13-17 slots até a EMA (α=0.2 @1Hz, atribuição = (ramUsed−baseline)/activeCount, baseline recapturado a cada dreno) corrigir — só morde em rajadas sincronizadas; (4) rampa PSI aditiva (+max(3,10%)/s a partir do teto-seed) + fast-ramp por run (ceil(busy·0.5)/500ms) = latência de partida; (5) histerese de admissão (3×500ms). pi mais leve só ajuda quando demanda ≥ teto. Knobs novos EVIDENCE-BASED com defaults intactos: `HUU_AGENT_MEM_SEED_MB` (clamp 128–2048) e `HUU_AGENT_MEM_EMA_ALPHA` (0.01–1) via `src/lib/ram-tuning.ts` (spread nos DOIS construtores: Orchestrator e GlobalScheduler; chaves omitidas preservam os `??` defaults), + observabilidade `dlog('scaler','config')` no start e `dlog('scaler','ema_move')` gated (|Δ|≥64MiB ou ≥10%) pra calibrar o seed com medição antes de baixá-lo (baixar sem medir foi a classe do incidente OOM).
