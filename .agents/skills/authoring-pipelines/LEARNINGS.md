# Learnings — authoring-pipelines

Append-only log consumed by meta-skill-evolution and meta-skill-consolidate.
Entry format: `- [YYYY-MM-DD][source:user|inference][task:<slug>][probation] <fact>`
States: probation (default) -> promoted (distilled into SKILL.md by meta-skill-consolidate after dual-buffer check) | superseded (kept for history, never deleted).
Learnings are routed here when THIS skill owns the domain of the fact — regardless of which skill ran the task.

<!-- entries below this line -->
- [2026-06-12][source:user][task:memory-scope][probation] Third file-selection mode shipped: scope "memory" + filesFrom (huu-memory-v1: {"_format":"huu-memory-v1","files":[{"path","hint?","priority?"}]}) — an earlier step writes the list, the step fans out one agent per path, $hint carries the producer's note. Rules: never the first step (schema-enforced), maxFiles default 40, config.files override wins, missing→empty stage / corrupt→fail. Docs: pipeline-json-guide "memory" section.
- [2026-06-12][source:user][task:dag-waves][probation] parallel/switch/join shipped: dependsOn (earlier-steps-only, waves), diamond pattern, activation-edge semantics for outcomes/next in DAG mode. Distilled into SKILL body v0.4.0.
- [2026-06-25][source:inference][task:autonomous-pipelines][probation] WHERE to apply `dependsOn` waves: only where parallel branches write SEPARATE outputs (Security Audit fans secrets/CVE/supply-chain into distinct report sections, joined by a consolidation step that `dependsOn` all of them). Do NOT force waves onto steps that share a SEQUENTIAL read-then-append blackboard (the progressive-knowledge FAQ): parallelizing them breaks "read what the prior step found" cross-references and multiplies merge-conflict surface for no real gain. Independent dimensions → waves; blackboard chains → linear.
- [2026-06-25][source:inference][task:autonomous-pipelines][probation] Native stub dry-run (no Docker, fast): in a temp git repo with a `{"backend":"stub"}` config, `HUU_NO_DOCKER=1 node_modules/.bin/tsx src/cli.tsx auto <pipeline> --config config.json` E2E-validates waves + memory + check loops (assert `"ok": true` + a `huu/<id>/integration` branch). Under stub, a recon `produces` step writes NOTHING, so the downstream `scope:'memory'` fan-out resolves to ZERO tasks (stub-safe) — the dry-run exercises topology + forward-default termination, not the per-file work. Perfect wiring smoke; pair with the full vitest run for the contract.
