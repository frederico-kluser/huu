# Learnings — writing-project-docs

Append-only log consumed by meta-skill-evolution and meta-skill-consolidate.
Entry format: `- [YYYY-MM-DD][source:user|inference][task:<slug>][probation] <fact>`
States: probation (default) -> promoted (distilled into SKILL.md by meta-skill-consolidate after dual-buffer check) | superseded (kept for history, never deleted).
Learnings are routed here when THIS skill owns the domain of the fact — regardless of which skill ran the task.

<!-- entries below this line -->
- [2026-06-24][source:inference][task:readme-competitor-rewrite][probation] The README version badge (`img.shields.io/badge/version-X.Y.Z`) is hand-maintained and drifts from `package.json` — found at 1.3.0 while package.json was 1.4.0. When editing either README twin, reconcile the badge against `node -p "require('./package.json').version"`.
- [2026-06-24][source:inference][task:readme-competitor-rewrite][probation] The README "Backends" table + mermaid drift from `src/orchestrator/backends/registry.ts` — both READMEs listed only pi/copilot/stub while the registry ships 4 (azure was production-ready but undocumented). Reconcile the backend list against the registry's `case '<kind>'` arms when touching that section.
- [2026-06-24][source:inference][task:readme-competitor-rewrite][probation] Competitor/positioning claims in the README age fast (star counts, project status, license, vendor mergers). Keep hard numbers (GitHub stars) OUT of committed docs — state ecosystem size qualitatively — and prefer the two-axis framing (who decides scope · how work is merged) which is stable as the landscape churns.
- [2026-06-25][source:inference][task:web-removal-maxmode-docsync][probation] `CLAUDE.md` is a SYMLINK to `AGENTS.md` (`ls -la` confirms; sections live at identical line numbers) — edit `AGENTS.md` once and both update; treating them as two files invites a self-conflict. Skills/router read `CLAUDE.md`, humans read `AGENTS.md`, same bytes.
- [2026-06-25][source:inference][task:web-removal-maxmode-docsync][probation] Syncing docs to a removal/refactor commit: three classes are FROZEN history — never rewrite. (1) Released CHANGELOG sections — a removal goes in `[Unreleased] → Removed`; the original `[X.Y.Z]` Added entry stays as shipped (v1.4.0 keeps its `--web`/web-UI lines). (2) `.agents/workbench/*.md` Fase-N analysis snapshots (dated "RASCUNHO/Checkpoint", reference prior skill sets) — only `.agents/workbench/skill-map.md` is LIVING. (3) `docs/merges/*.md` run logs. Also: pattern sweeps miss bare-"web" in prose ("judge cards in TUI and web", "ativa nos dois modos") — also grep `\bweb\b` and `\bboth modes\b`/`dois modos`, then filter web.dev/web-vitals/"fetched web".
- [2026-06-25][source:inference][task:card-action-counters][probation] Agent/kanban CARD display changes have TWO independent renderers: `src/ui/components/RunKanban.tsx` (Ink TUI) and `src/web/client` (vanilla JS). Adding a field to `AgentStatus` makes the DATA available to both (it rides the SSE payload + `RunManifest`), but the web client does NOT render it unless its JS is also edited — a RunKanban-only change is TUI-only. So document such features as the terminal UI (`huu --cli`) and do NOT add them to the README's "clickable kanban / Tudo clicável" bullet, which describes the WEB UI specifically. CHANGELOG + docs/ARCHITECTURE.md (the "Logs and debugging" kanban description) + AGENTS.md kanban-state paragraph are the right homes for a TUI-card feature; the README stays web-first.
- [2026-06-25][source:inference][task:web-removal-maxmode-docsync][probation] A feature commit that only appends `[probation]` LEARNINGS entries leaves the skill BODIES + `AGENTS.md`/`CLAUDE.md` + user docs (README twins, operations.{md,pt-BR.md}, ARCHITECTURE.md, onboarding twins) STALE. Enumerated-fact phrasings silently rot: "runs in BOTH modes" / "in both concurrency modes" / "nos dois modos" all became wrong when a 3rd auto-scale mode (greedy/MAX) landed. Grep those exact phrasings across every doc twin, not just the one the commit touched (it touched only docs/KEYBOARD.md).
- [2026-06-26][source:inference][task:realtime-cost-and-log-fixes][probation] When a fix DELIVERS a previously-documented caveat/roadmap item, one fact is usually mirrored in MORE than the obvious twin pair — for `totalCost` it lived in SIX spots: the web "About/Sobre custo" note, the headless "Cost caveat", AND the "Roadmap" table — in BOTH `README.md` and `README.en.md` — plus the owning skill body (`building-web-ui` "Cards & cost"). The Roadmap table row is the easy miss. Grep the CODE MARKER the docs quote (here `// M5 will populate`, plus `totalCost`) across `README.md README.en.md .agents/skills/*/SKILL.md docs/` BEFORE editing, then move the item from 🧭 Roadmap to ✅ Implemented in BOTH twins and replace each caveat with the real behavior. CHANGELOG gets the user-visible bullet under `[Unreleased]` (Fixed for the bug, Changed/Added for new behavior). Removing a `// marker`-style code comment also orphans any doc that cites it verbatim.
