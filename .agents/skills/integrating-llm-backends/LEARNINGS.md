# Learnings — integrating-llm-backends

Append-only log consumed by meta-skill-evolution and meta-skill-consolidate.
Entry format: `- [YYYY-MM-DD][source:user|inference][task:<slug>][probation] <fact>`
States: probation (default) -> promoted (distilled into SKILL.md by meta-skill-consolidate after dual-buffer check) | superseded (kept for history, never deleted).
Learnings are routed here when THIS skill owns the domain of the fact — regardless of which skill ran the task.

<!-- entries below this line -->
- [2026-06-25][source:user][task:openrouter-key-401-shadow][promoted] "Valid key still 401s" is almost always the resolver picking a DIFFERENT key than the one the user set: env var (step 3) outranks the Options/config.json store (step 4), so a stale `OPENROUTER_API_KEY` (commonly exported from a shell profile or a sourced `~/.secrets`) shadows what the user saved. Diagnose with `checkOpenRouterReachable` vs a raw `curl https://openrouter.ai/api/v1/auth/key` — same endpoint, so a 200 on curl + 401 in huu means key MISMATCH, not a bad key. `dev`/`start` run native (`HUU_NO_DOCKER=1`), so the resolver sees the host env directly.
- [2026-06-25][source:inference][task:openrouter-key-401-shadow][promoted] Added `resolveApiKeyWithSource(spec) -> {value, source, shadowsStored}` + `keyRemedyHint(spec, res)` in `api-key.ts` (resolveApiKey now delegates; value unchanged). The orchestrator 401 path and the docker-reexec host loop use them to name the ACTUAL source instead of the misleading "update it in Options" (a no-op when an env var shadows the saved key). `shadowsStored` = a higher tier won AND a different non-empty key sits in the store.
- [2026-06-25][source:user][task:openrouter-key-401-shadow][promoted] Web UI key policy chosen by the user: BROWSER-ONLY, never disk. Flow: browser `POST /api/keys/validate` (validates via `validateKeyValue` in `api-data.ts` — openrouter→checkOpenRouterReachable, azureApiKey→checkAzureReachable, else unverifiable) → on `valid`/`unverifiable` keep the value in `sessionStorage('huu.key.<spec>')` → send it as `apiKey` in `POST /api/run` → `run-manager` prefers `params.apiKey` over `resolveApiKey`, NEVER calls `saveApiKey`. `invalid` (401/403) is refused at entry. `BackendInfo.apiKeySpecName` was added so the browser can look up its per-backend session key. The legacy `POST /api/keys` (disk) endpoint stays for CLI reuse but the browser no longer calls it.
- [2026-06-25][source:user][task:pi-only-provider-and-blob][probation] huu is now PI-ONLY at the UI level: `AgentBackendKind` is `'pi' | 'azure' | 'stub'` (copilot fully removed — dir, `@github/copilot-sdk` optionalDep, spec, models, `--copilot`). The user-facing choice is `LlmProvider = 'openrouter' | 'azure'`, modeled in a NEW lib module `src/lib/providers.ts` (`PROVIDERS`, `providerInfo`, `providerToBackend` openrouter→pi/azure→azure, `backendToProvider` stub→openrouter, `parseProvider`). Azure stays a real internal dispatch kind + factory (its plumbing/tests untouched) but `userSelectable:false` — reached via the provider toggle, NOT a standalone backend. Lowest-risk way to honor "only pi, pick provider": map provider→backend at launch, leave the factory/catalog/key layers backend-keyed. providers.ts lives in lib so UI + web + api-key can import it without an upward dependency.
- [2026-06-25][source:inference][task:pi-only-provider-and-blob][probation] `findMissingKeysForBackend` was widened to accept the full `AgentBackendKind` (stub → `[]`) because `providerToBackend` returns the union; added `findMissingKeysForProvider(p)` as the provider-keyed wrapper. The web exposes provider truth via `listProvidersInfo()` (id, backend, label, per-spec presence) and `/api/models`+`/api/keys` accept `?provider=` (mapped to backend) alongside the legacy `?backend=`. `api-key-registry.backendBound` is now `'pi' | 'azure'`. Removing copilot is a wide but mechanical sweep — grep `copilot` across src AND the four regression tests (registry/catalog/api-key/screen-fsm) that pin the backend list.
- [2026-06-25][source:inference][task:merge-gifted-archimedes][probation] When the pi-only/provider branch met the browser-only-key branch on main, the web layer (`server.ts`, `run-manager.ts`, `api-data.ts`) auto-merged to support BOTH at once: provider selection AND the browser-only key flow. The client (`app.js`/`index.html`) is the reconciliation point — keep the branch's provider segmented control + per-spec `renderKeyArea`, but wire each key row's Save to `POST /api/keys/validate` + `sessionStorage` (NOT the disk `POST /api/keys`), and send `apiKey`/`endpoint`/`runDirectory` on `/api/run`. `backendSpecName(S.backend)` still resolves the primary spec because bootstrap returns `backends` alongside `providers`.
- [2026-06-25][source:user][task:invert-key-precedence][probation] SUPERSEDES the precedence half of the two `[task:openrouter-key-401-shadow]` entries above: the resolver was INVERTED so the explicitly SAVED store now OUTRANKS the env var. New order in `resolveApiKeyWithSource`: secret-mount → **stored** → env-file → env → none. The `shadowsStored` field was renamed to `storedOverridesEnv` (true only when `source==='stored'` AND a different non-empty ambient env value exists — an ignored env var). `keyRemedyHint`'s foot-gun message moved from `case 'env'` to `case 'stored'` (env now only reached as the fallback when nothing is saved). Rationale: explicit user choice beats ambient shell leakage; CI is unaffected (no Options save → store empty → env still wins) and Docker is safe (`~/.config` isn't mounted, so in-container the secret-mount carries the host-resolved key). The web already preferred the browser key (`run-manager.ts` `params.apiKey || resolveApiKey`), so only the terminal resolver changed. Consumers updated: `docker-reexec` host note (`storedOverridesEnv`), `OptionsScreen` ⚠ row, `orchestrator/index.ts` 401 hint, `api-key.test.ts`.
- [2026-06-25][source:user][task:web-model-selector-openrouter][probation] The WEB model picker is vanilla JS (`src/web/client/app.js` + `index.html`), fed by `GET /api/models` → `listModelsForBackend(cwd, backend, openrouterKey)` in `api-data.ts` — it is NOT the Ink TUI path (`ModelSelectorOverlay` + the `model-selector-ink` "More models" table). "Add models to the selector" is ambiguous: confirm web vs TUI FIRST (this task pivoted from TUI to web on the user's clarification). Feature shipped: for backend `pi` WITH a key, `/api/models` returns the LIVE OpenRouter catalog filtered to models whose `supported_parameters` ⊇ {`tools`,`reasoning`} (`filterToolReasoningModels`/`listToolReasoningModels` in `lib/openrouter.ts`; ~170 today), mapped to `ModelInfo` (+ optional `contextLength`); no key → static recommended. GATE the live fetch on a NON-EMPTY key so `server.test.ts`'s `/api/models?backend=pi` stays hermetic (no header → no outbound fetch → recommended array). The browser sends its validated `sessionStorage` key via the `x-huu-key` request HEADER (not a query param — avoids access logs); `OpenRouter /api/v1/models` is public so listing also works keyless, but gating on key honors the browser-only flow + keeps boot fast. NOTE: `app.js`'s `api()` wrapper had to merge `opts.headers` — it previously let `...opts` clobber the default Content-Type/`x-huu-token` headers. Verified E2E against the real key: 170 live, all `thinking`; 684 tests green.
- [2026-06-25][source:inference][task:run-log-realtime-stream][probation] The pi event-mapper (`backends/pi/event-mapper.ts`) historically consumed `message_update` NOT AT ALL — only agent_start / tool_* / message_end(usage) / agent_end / retry. But `session.subscribe` (factory.ts) receives EVERY `AgentSessionEvent`, and during generation pi fires MANY `message_update`s, each carrying `assistantMessageEvent` (a pi-agent-core `AssistantMessageEvent`: `text_delta`/`thinking_delta`/`{text,thinking,toolcall}_{start,end}`/start/done/error, each with a cumulative `partial: AssistantMessage`). Dropping them meant the agent's actual streamed reply+reasoning was captured NOWHERE and the run log only advanced at tool boundaries (looked frozen mid-generation — the "run log only works when I click stop" report). Fix: map `message_update` → new AgentEvent `{type:'stream',channel:'assistant'|'thinking',delta}` for `text_delta`/`thinking_delta` ONLY (cumulative `partial` + structural start/end/toolcall sub-events are log noise). `message_end` stays the usage/token source (unchanged); file WRITES go through the tool path, so assistant text is prose/narration, not file dumps. Backend-agnostic seam: any backend can now emit `stream` and get the live run log + console firehose for free (the stub does, which is how the web SSE test exercises it without a real LLM).
- [2026-06-26][source:user][task:openrouter-models-keyless-public-catalog][probation] SUPERSEDES the GATE half of `[task:web-model-selector-openrouter]` and the "hermetic gate unchanged" note in `[task:openrouter-all-models-freetext]`: the web picker NO LONGER gates the live fetch on a non-empty key. User report: clicking "Model" showed only the ~2 static presets until a key was validated. Root cause: `listModelsForBackend` had `if (backend === 'pi' && key)` → no key → `recommended-models.json` fallback. Fix: `if (backend === 'pi')` (key optional, passed through). OpenRouter's `GET /api/v1/models` is PUBLIC — confirmed by `curl https://openrouter.ai/api/v1/models` with NO auth header → HTTP 200, 339 models (255 tools, 196 reasoning). `buildAuthHeaders` now OMITS `Authorization` when the key is empty (an empty `Bearer ` is rejected) and still sends `Bearer <key>` when present; `fetchModelCapabilities`/`listAllModels` default `apiKey=''` (the '' key is its own public-catalog cache entry). Recommended list is now ONLY a network-failure fallback. The SKILL.md "Models" body line "gate the live fetch on a non-empty key so the endpoint stays hermetic in tests" is now STALE/WRONG — consolidate should rewrite it to: "fetch the PUBLIC catalog with OR without a key; keep tests hermetic by intercepting the `openrouter.ai` URL (not by a key gate)." Hermeticity preserved via the server-test pattern below. Verified E2E: booted `--web --stub`, `GET /api/models?provider=openrouter` with NO key → `source:openrouter-live`, 339 models incl. `deepseek/deepseek-chat`; full suite 704 green.
- [2026-06-25][source:user][task:openrouter-all-models-freetext][probation] SUPERSEDES the FILTER half of `[task:web-model-selector-openrouter]` above: the web picker no longer hard-filters the live catalog to `supported_parameters ⊇ {tools,reasoning}`. `listModelsForBackend` now calls the NEW `listAllModels`/`projectAllModels` (`lib/openrouter.ts`) which downloads the ENTIRE catalog (339 today vs the 170 dual-capable; 255 have `tools`), sorted by id, each projected with `supportsTools`/`supportsReasoning` booleans on `OpenRouterModelOption`. `ModelInfo` gained `tools?` and `thinking` now reflects the REAL `reasoning` flag (was hardcoded `true` under the filter). Rationale (user): the filter silently dropped models the user wanted (`deepseek/deepseek-chat`, brand-new ids) and "deepseek v4 pro/flash don't exist in our filter". `filterToolReasoningModels`/`listToolReasoningModels` are KEPT (tested) for callers wanting only the dual subset. The SKILL.md "Models" body line "filtered to `supported_parameters ⊇ {tools, reasoning}`" is now STALE — consolidate should rewrite it to "full catalog, capability-annotated; agents still REQUIRE tools so the picker badges `no tools` rather than hiding". The hermetic-test gate (no key → no outbound fetch → recommended) is unchanged; `api-data.test.ts`'s live-catalog test now asserts ALL models returned + per-model tools/thinking flags. Verified: 701 tests green; public `GET /api/v1/models` confirms deepseek-v4-pro/flash present with tools+reasoning.
- [2026-06-26][source:inference][task:default-model-deepseek-v4-flash][promoted] `loadRecommendedModels` (`src/models/catalog.ts`) SWALLOWS any zod parse error of `recommended-models.json` in a try/catch and silently returns the 2-entry in-code `DEFAULT_RECOMMENDED_MODELS` — so ONE out-of-enum value kills the WHOLE file with no log. The shipped file had been doing exactly this: it carried `tier:"planning"` + `bestFor:["planning",...]` that were NEVER added to `ModelTierSchema`/`ModelUseCaseSchema` (`src/contracts/models.ts`), so the documented planning models (deepseek-v4-pro, gpt-5.4, claude-opus-4.6 — see CHANGELOG/KEYBOARD.md) loaded for NOBODY and the real default silently became `minimax/minimax-m2.7`. Detect with `RecommendedModelsFileSchema.safeParse(JSON.parse(readFileSync('recommended-models.json')))`. Fix: added `'planning'` to BOTH enums + a `catalog.test.ts` regression that fails if the shipped file ever stops parsing. RULE: any edit to `recommended-models.json` must round-trip through the schema, and new `tier`/`bestFor` vocabulary REQUIRES the enum change first (`tier`/`bestFor` are cosmetic-only — rendered in the assistant prompt `· tier:` line + passed to the web `ModelInfo`; no code branches on the value, so adding an enum member is purely additive).
- [2026-06-26][source:user][task:default-model-deepseek-v4-flash][promoted] There is NO historical "default model" constant — the default was a CONVENTION: the FIRST entry of the recommended catalog (the TUI `ModelSelectorOverlay` quick-list headline under "── Recommended ──") and `S.models[0]` in the web client. User set the default to `deepseek/deepseek-v4-flash` (resolved via the PUBLIC `curl https://openrouter.ai/api/v1/models` — each entry has `id`/`canonical_slug`/`name`; flash = `deepseek/deepseek-v4-flash`, $0.09/$0.18 per M, 1M ctx, tools+reasoning, ~5× cheaper than v4-pro). Made it authoritative in THREE synced places: new exported `DEFAULT_MODEL_ID` in `catalog.ts` (single source), the FIRST entry of `DEFAULT_RECOMMENDED_MODELS` (in-code fallback, id set to `DEFAULT_MODEL_ID`), and the FIRST entry of `recommended-models.json` (shipped catalog). v4-pro KEPT as a `planning`-tier model. NOTE `recommended-models.json` is NOT materialized by `pipeline-bootstrap` (nothing in src WRITES it — verified by grep); it's a committed repo file READ by `loadRecommendedModels(projectRoot)` from the CWD (the audited project). So a normal user project (no such file) gets the in-code `DEFAULT_RECOMMENDED_MODELS` → flash-first; the shipped file only governs huu-against-huu dogfooding; the web `pi` path uses the LIVE OpenRouter catalog (flash is in it) → preselects flash regardless. A user only overrides the default by hand-placing their OWN `recommended-models.json` (their catalog wins, by design).
- [2026-06-26][source:inference][task:realtime-cost-and-log-fixes][probation] OpenRouter cost accounting (researched vs live docs/API, June 2026) — for a live per-agent cost meter the authoritative signal is `usage.cost`, returned on EVERY chat-completion (USD credits; in the LAST SSE chunk when streaming, in the full response otherwise) and already net of caching/BYOK/rounding. The opt-in flags `usage:{include:true}` and OpenAI-style `stream_options:{include_usage:true}` are now DEPRECATED/no-effect (usage is always returned). Do NOT bill from tokens×price — local math drifts (native-tokenizer counts + cache discounts + per-request fees); `/api/v1/models` `pricing.{prompt,completion,…}` are USD-per-token STRINGS with fields OMITTED (not zeroed) when N/A → use only for pre-flight estimates. `GET /api/v1/generation?id=` (total_cost, native_tokens_*, cache_discount which can be NEGATIVE on cache writes) is async/audit-only. `GET /api/v1/credits` → {total_credits,total_usage} for an optional balance readout. huu already does the right thing: the pi mapper reads `usage.cost?.total` (`event-mapper.ts` message_end) → emits the `usage` AgentEvent `cost` → `handleAgentEvent` accumulates into `AgentStatus.cost`. So summing `AgentStatus.cost` (the getState fix) is correct, and future merge/judge metering only needs those agents' usage events captured the same way.
- [2026-06-30][source:inference][task:deep-research-multirun-oom-pause][probation] pi-coding-agent (v0.73) HAS native session persistence/resume that huu does NOT use — the lever for "pause the agent instead of killing it" under memory pressure. The factory (`backends/pi/factory.ts`) uses `SessionManager.inMemory()` (disposable per task, transcript only in heap), but the SDK also exposes `SessionManager.create(cwd, sessionDir)` which PERSISTS the transcript to disk on every `message_end` (versioned session format `CURRENT_SESSION_VERSION = 3`: `SessionHeader` + JSONL entries), `switchSession` ("used for resume and branching"), a `--resume` CLI flag, and `AgentState.messages` + `serializeConversation()` for serialization. KEY CONSTRAINT confirmed against `docs/pi-coding-agent.md` §1 + factory: agents run IN-PROCESS (every pi session shares huu's single Node heap; isolation is git/fs/ports, NOT an OS process), so there is NO per-PID kill / SIGSTOP / per-agent cgroup, and suspending only the async loop does NOT free RAM. "Pause without losing work" is therefore achievable only via COOPERATIVE CHECKPOINT: abort at a turn boundary (between `message_end`s) → persist session → `dispose()` (frees heap) → later recreate the session from its file + the SAME worktree and continue. That would let the memory-guard PRESERVE work (suspend) instead of kill+requeue-from-scratch (which today loses BOTH the LLM context and the partial worktree). Resume re-pays INPUT tokens (mitigable by prompt caching) but does not re-derive or re-run applied tools. Per-agent footprint measurement (`/proc/<pid>`, cgroup `memory.current`) likewise presupposes making agents subprocesses. Written up as research block G in `~/Downloads/huu-deep-research-escalabilidade-multirun.md` (a deep-research PROMPT, not a repo doc).
- [2026-07-01][source:inference][task:dogfood-angular-react-multirun][probation] Debugging an OpenRouter 401 in a huu run: (1) OpenRouter `GET /api/v1/models` is a PUBLIC endpoint — it returns 200 for ANY key (even garbage), so a 200 there does NOT prove a key is valid. To actually test a key use `GET https://openrouter.ai/api/v1/key` (200 = valid, 401 = invalid) or a tiny `POST /api/v1/chat/completions`. (2) The key resolver (`src/lib/api-key.ts resolveApiKeyWithSource`) ranks tiers secret-mount > STORED > env: the explicitly-saved key in `~/.config/huu/config.json` (a JSON object keyed by provider name, e.g. `{"openrouter":"sk-or-…"}`) OUTRANKS `OPENROUTER_API_KEY`. (3) The run-time 401 message ("huu used the key you saved in the Options screen … OPENROUTER_API_KEY is IGNORED while a saved key exists") is a heuristic diagnostic and CAN MISATTRIBUTE blame — in a web run the browser/caller-supplied `apiKey` in the POST body is what actually gets used, so a stale key passed in the body 401s while the message fingers the (valid) stored key. When stuck, test each candidate key value against `/api/v1/key` directly rather than trusting the diagnostic.
- [2026-07-01][source:user][task:hermetic-pi-runtime][probation] huu agora compõe TODA sessão pi hermeticamente via `src/orchestrator/backends/pi/hermetic.ts` (`buildPiSessionEnvironment`) — default ON, escape `HUU_PI_HERMETIC=0`. Fatos duros do SDK 0.73.1 que sustentam o seam: (1) `createAgentSession` preenche `settingsManager`/`resourceLoader`/`agentDir` omitidos com defaults HOST-GLOBAIS — `SettingsManager.create(cwd, ~/.pi/agent)` lê settings global+projeto, cuja lista `packages` resolve via `npm root -g` e CARREGA extensões npm globais dentro de agentes headless (a rota do crash pi-animations); `DefaultResourceLoader` ainda escaneia `~/.pi/agent/{extensions,skills,prompts,themes}`, `cwd/.pi`, `~/.agents/skills` de TODOS os ancestrais, e injeta AGENTS.md/CLAUDE.md de todo diretório até `/`. (2) O SDK **NÃO recarrega loader fornecido pelo caller** (sdk.js: só o default ganha `await reload()`) — quem injeta `resourceLoader` TEM que `await loader.reload()` antes. (3) NÃO vale stub custom de `ResourceLoader`: `getExtensions()` exige um `ExtensionRuntime` não-trivial — frágil a versão; o caminho robusto é `DefaultResourceLoader` com as flags `noExtensions/noSkills/noPromptTemplates/noThemes/noContextFiles` + `agentsFilesOverride` (existe nativo! aplica POR CIMA de `noContextFiles:true` — resource-loader.js:322-326) pro contexto escopado. (4) `AuthStorage.create()` sem path LÊ (e cria se faltar) `~/.pi/agent/auth.json` mesmo com `setRuntimeApiKey` — use `AuthStorage.inMemory()`; idem `ModelRegistry.create()` → `models.json` (use `.inMemory(auth)`). (5) `PI_CODING_AGENT_DIR` é o override oficial do agent dir (config.js) — exportado só-se-unset como defesa. (6) **azure/factory.ts também é sessão pi** (mesmo `createAgentSession`) — o seam serve os dois backends e os conflict-resolvers de graça (registry reusa os factories). (7) Pin EXATO 0.73.1 (sem caret) porque o hermético depende dos option-names; os canários de `hermetic.test.ts` (HOME sandbox + canário de auth/skill/packages + snapshot da árvore `.pi`) quebram alto num bump regressivo.
