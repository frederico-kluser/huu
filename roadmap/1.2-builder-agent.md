# 1.2 Builder Agent (first agent)

> Phase 1 — Single Agent Loop (Act 1 → Act 2)

The first concrete agent. Proves the runtime works end-to-end: receive task, create worktree, implement code, commit, signal done.

## Checklist

- [ ] Builder agent definition (Sonnet, Read/Write/Edit/Bash tools)
- [ ] End-to-end test: receive subtask → create worktree → implement → commit → signal done
- [ ] File change tracking (which files were created/modified/deleted)

## Dependencies

- 1.1 Agent Runtime (spawner, tools, lifecycle)

## Outputs

- `src/agents/definitions/builder.ts` — builder agent definition
- End-to-end test proving the full agent → worktree → commit pipeline
