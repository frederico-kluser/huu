# 3.4 Specialized Views

> Phase 3 — TUI (Act 2 → Act 3: Resolution)

Dedicated tabs for specific concerns: logs, merge queue, cost tracking, and beat sheet progress.

## Checklist

- [ ] `[L]ogs` tab — aggregated log view from all agents
- [ ] `[M]erge Queue` tab — FIFO queue with tier indicators
- [ ] `[C]ost` tab — breakdown by agent, model, phase
- [ ] `[B]eat Sheet` tab — hierarchical progress view with checkpoints

## Dependencies

- 3.1 Kanban Board (tab navigation shell)
- 2.1 Beat Sheet Engine (beat sheet data for B tab)
- 1.3 Merge Workflow (merge queue data for M tab)

## Outputs

- `src/tui/views/LogsView.tsx` — aggregated logs from all agents
- `src/tui/views/MergeQueueView.tsx` — FIFO queue visualization
- `src/tui/views/CostView.tsx` — cost breakdown by agent/model/phase
- `src/tui/views/BeatSheetView.tsx` — hierarchical progress with checkpoint indicators
