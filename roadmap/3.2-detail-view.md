# 3.2 Detail View

> Phase 3 — TUI (Act 2 → Act 3: Resolution)

Deep dive into a single agent's work. Live logs, diffs, metrics — everything you need to understand what an agent is doing.

## Checklist

- [ ] Live log streaming from agent execution
- [ ] Diff preview (files changed by agent)
- [ ] Metrics panel: tokens used, context %, cost, elapsed
- [ ] Context usage bar (visual)
- [ ] ESC to return to Kanban

## Dependencies

- 3.1 Kanban Board (navigation to detail view via ENTER)
- 1.1 Agent Runtime (log streaming)

## Outputs

- `src/tui/components/DetailView.tsx` — full detail view
- `src/tui/components/LogStream.tsx` — live log component
- `src/tui/components/DiffPreview.tsx` — file changes visualization
- `src/tui/components/MetricsPanel.tsx` — tokens, cost, context bar
