# 5.3 Performance

> Phase 5 — Polish (Final Image)

Tuning for real-world usage. Concurrency limits, coordination overhead measurement, and resource optimization.

## Checklist

- [ ] Agent concurrency cap (configurable, default 5)
- [ ] Coordination overhead metrics visible in TUI
- [ ] SQLite query optimization for large histories
- [ ] Worktree reuse (recycle instead of create/destroy)

## Dependencies

- 2.2 Orchestrator Loop (concurrency control)
- 3.4 Specialized Views (overhead metrics in Cost tab)
- 0.3 WorktreeManager (worktree recycling)

## Outputs

- Configurable concurrency cap with backpressure
- Coordination overhead metric: time spent coordinating vs. time agents spend working
- SQLite indexes and query optimization for tables with >10K rows
- Worktree pool: recycle existing worktrees instead of create/destroy per task

## Technical Notes

- Overstory's STEELMAN.md: above ~20 agents, coordination cost exceeds parallelism gains
- Default cap of 5 concurrent agents balances throughput vs. coordination
- Worktree reuse avoids repeated `git worktree add/remove` overhead
