# 2.3 Remaining Agents

> Phase 2 — Orchestration (Act 2: Confrontation)

The full roster. 9 additional agents beyond the builder, completing the Extended Squad of 11.

## Checklist

- [ ] `planner` — Beat Sheet decomposition (Sonnet)
- [ ] `tester` — TDD + test execution (Sonnet)
- [ ] `reviewer` — code review, read-only tools (Opus)
- [ ] `researcher` — web search + context gathering (Haiku)
- [ ] `merger` — conflict resolution agent (Sonnet)
- [ ] `refactorer` — cleanup + dead code removal (Haiku)
- [ ] `doc-writer` — documentation sync (Haiku)
- [ ] `debugger` — deep bug investigation (Opus)
- [ ] `context-curator` — post-activity memory curation (Haiku)

## Dependencies

- 1.1 Agent Runtime (spawner and lifecycle)
- 1.2 Builder Agent (pattern to follow for definitions)

## Outputs

- `src/agents/definitions/` — one file per agent definition
- Agent-specific tool configurations and prompts

## Technical Notes

- Model tiering: Opus ($15/MTok) for critical decisions, Sonnet ($3/MTok) for work, Haiku ($0.80/MTok) for mechanical tasks
- Reviewer is read-only (Read, Grep, Glob) — never writes code
- Context-curator runs post-activity, not during
