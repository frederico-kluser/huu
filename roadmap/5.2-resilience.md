# 5.2 Resilience

> Phase 5 — Polish (Final Image)

Crash recovery, timeout handling, and graceful shutdown. The orchestrator must survive failures without losing work.

## Checklist

- [ ] Crash recovery: resume from last SQLite checkpoint
- [ ] Stale worktree detection and cleanup
- [ ] Agent timeout with automatic retry
- [ ] Graceful shutdown (finish current agents, merge completed work)

## Dependencies

- 0.2 SQLite Schema (checkpoint data)
- 0.3 WorktreeManager (stale worktree detection)
- 2.2 Orchestrator Loop (shutdown hooks)

## Outputs

- `src/orchestrator/recovery.ts` — crash recovery from SQLite state
- `src/git/cleanup.ts` — stale worktree detection and removal
- Signal handlers (SIGINT, SIGTERM) for graceful shutdown
- Agent timeout configuration with retry policy
